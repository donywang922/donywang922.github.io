# 超量

“超量”接受两个同量纲、有序的量 `A`、`B`，取得 `A` 超过 `B` 的部分：

```text
超量(A, B) = max(A - B, 0)
```

当 `A` 大于 `B` 时，结果为正差；当 `A` 小于或等于 `B` 时，结果为该量接口的零。任一输入无结果时，结果也无结果。

对有效输入而言，`判空(超量(A, B))` 表示 `A ≤ B`，再接“非”则表示 `A > B`。需要区分无结果与零值时，应先判定输入来源是否有效。
