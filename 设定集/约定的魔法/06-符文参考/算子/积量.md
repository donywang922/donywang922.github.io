# 积量

“积量”接受两个标量 `A`、`B`，产生两者的乘积：

```text
积量(A, B) = A × B
```

结果量纲为两输入量纲之积。两个无量纲数相乘仍是无量纲数，长度与长度相乘可得到面积量，长度与时间的倒数量相乘可得到速度量。位移和方向不是标量，不能进入积量；位移乘无量纲数使用“缩放”。

任一输入无结果时，结果也无结果；有效的零量正常得到零。重复使用积量并配合递归，可以表达整数次乘方。
