# 转向

“转向”是三输入几何算子，依次接受待旋转的方向或位移 `V`、旋转轴方向 `D` 与角度 `A`：

```text
转向(V, D, A)
```

结果绕 `D` 旋转 `A`。正角方向由最终指令坐标上下文的定向和 `D` 的正向共同确定。`V` 为方向时结果仍是方向；`V` 为位移时结果仍是位移并保持长度。

三项均须来自现实或由现实关系计算得到。坐标绕现实支点旋转时，先以坐标差取得相对位移，经转向旋转后，再以移位还原为坐标。任一输入无结果时，结果也无结果。
