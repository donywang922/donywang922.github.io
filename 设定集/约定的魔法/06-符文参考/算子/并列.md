# 并列

“并列”接受两个元素接口完全相同的序列 `A`、`B`，产生先列出 `A` 当前各项、再列出 `B` 当前各项的序列：

```text
并列(A, B)
```

结果保留共同的元素接口，并实现等于两输入当前项数之和的计数量。两序列的元素接口须完全相同，否则算子成型失败。

并列保存连接关系而非元素副本。输入序列以后改变时，结果在实际取值时随之改变。任一输入为空时按空序列处理，因此另一输入的当前序列仍可成为结果；两者都空时结果为空序列。
