# 空式

> 编写状态：语义已经确认；具体字形尚待确定。

“空式”算子不需要输入，成型后产生一个**空式引用**。空式引用实现指令接口，但不包含任何指令。

空式引用描述的是由真实符文构成的空指令结构，不是数据常量；约定仍不存在凭空产生数字、判定或其他数据值的算子。

执行空式不会改变世界，也不会增加执行献祭需求。它可以用于表示不采取动作、作为递归的终止结果，或与其他指令引用接续。

空式没有执行代价，不表示它可以无代价形成。空式算子成功成型时，仍会献祭该算子实际标注过的时空。
