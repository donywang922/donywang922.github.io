# 截列

“截列”是三输入序列算子，依次接受序列 `S`、起始序号 `B` 与项目数量 `N`：

```text
截列(S, B, N)
```

`B` 与 `N` 必须是非负无量纲整数，序号从零开始。范围有效时，结果为从 `S[B]` 开始的连续 `N` 项，并保留 `S` 的元素接口；`N` 为零时结果为空序列。

起点或数量不合法、所要求范围越过当前序列末端，或任一输入无结果时，结果为空序列。零起点表示从首项开始，零数量得到空序列。跳跃、反向和循环读取可由规则、取项与闭合组合表达。
