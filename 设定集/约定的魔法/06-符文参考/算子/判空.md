# 判空

> 编写状态：名称和基本语义已经确定；各计算接口采用什么默认值或空值，以及具体字形和输入位置尚待确定。

“判空”接受一个实现可空接口的引用 `A`，产生一个判定引用：

```text
判空(A)
```

所得引用在实际取值时读取 `A` 的当前结果。若该结果是相应接口规定的默认值或空值，判定成立；否则判定不成立。

判空不会保存成型时的结果，也不是相等比较。每个可空接口只能规定一种确定的空值含义；判空不接受两个输入，也不能用于比较两个引用。

判空算子成功成型时，献祭该算子实际标注过的时空。其判定参与消解时不单独增加执行献祭。
