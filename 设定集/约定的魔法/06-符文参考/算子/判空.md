# 判空

“判空”接受一个具有可空主值的引用 `A`，产生一个判定引用：

```text
判空(A)
```

所得引用在实际取值时读取 `A` 的当前主值。若该主值是相应接口规定的默认值或空值，判定成立；否则判定不成立。完整表见[数据来源与空值](../../03-语言结构/数据来源与空值.md)。

每个可空引用都有一个供判空检查的主值。判定在实际取值时根据该主值的当前状态产生。

判空算子成功成型时，献祭该算子实际标注过的时空。其判定参与消解时不单独增加执行献祭。
