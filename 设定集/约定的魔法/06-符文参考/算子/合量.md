# 合量

“合量”接受两个实现同一可加量接口、量纲相同的引用 `A`、`B`，产生：

```text
合量(A, B) = A + B
```

数、长度、时间、角度和位移都可以在各自封闭的可加接口中使用合量。坐标不实现可加量接口；坐标与位移的组合使用“移位”。两输入量纲或几何种类不相容时，算子在成型时失败。

任一输入实际取值无结果时，结果也无结果；有效的零量仍正常参加相加。否则主值为相加结果，默认值为该接口的零。合量只保存关系，不保存成型时的值。
