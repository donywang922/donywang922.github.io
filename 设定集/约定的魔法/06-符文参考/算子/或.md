# 或

> 编写状态：语义已经确认；具体字形和输入位置尚待确定。

“或”接受两个判定引用 `A`、`B`，产生新的判定引用：

```text
或(A, B)
```

任一输入能够明确判为成立，结果即成立；只有两者都能明确判为不成立，结果才不成立。求值不以第一输入为先，因此一项无法终止、另一项明确成立时，仍可得到成立。

完整的高维确定性求值规则见[判定与确定性求值](../../03-语言结构/判定与确定性求值.md)。算子成功成型时献祭其实际标注时空，取值时不增加执行献祭。
