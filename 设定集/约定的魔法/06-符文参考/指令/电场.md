# 电场

## 句式与位置

“电场”的指令主体依次写入电场主符和强度符。主符尺寸决定影响范围，强度符尺寸决定状态改变量。指令在电场引用于执行瞬间所在的位置生效。

## 效果

指令执行时，约定一次性选中效果框空间范围内的全部原子，并沿指令主向修改它们与电场类效果对应的瞬时物理状态。

结算完成后，原子的电磁响应、运动、辐射或材料变化从修改后的状态继续演化。

重复执行电场会在每一次执行时重新读取当时范围内的原子，并在已有状态上继续结算。每次执行都单独计入整次术式的执行献祭。
